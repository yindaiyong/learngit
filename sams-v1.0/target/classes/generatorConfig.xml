<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<!-- 使用 mvn mybatis-generator:generate -->
<generatorConfiguration>
    <!-- 配置文件 -->
    <properties resource="jdbc.properties"/>
    <context id="shiroTables" targetRuntime="MyBatis3" defaultModelType="flat">

        <!-- plugin -->
        <plugin type="org.mybatis.generator.plugins.SerializablePlugin"/>
        <plugin type="org.mybatis.generator.plugins.ToStringPlugin"/>

        <!-- 取消生成的代码注释 -->
        <commentGenerator>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <!-- 数据库链接URL、用户名、密码 -->
        <!-- Mysql:"com.mysql.jdbc.Driver" Oracle:"oracle.jdbc.driver.OracleDriver" -->
        <jdbcConnection driverClass="oracle.jdbc.driver.OracleDriver" connectionURL="${master_driverUrl}" 
                        userId="${master_username}"  password="${master_password}">
        </jdbcConnection>

        <!--1、生成Java模型的包名和位置 -->
        <javaModelGenerator targetPackage="com.sams.custom.model" targetProject="src/main/java">
            <property name="trimStrings" value="true"/>
        </javaModelGenerator>

        <!--2、生成的映射文件Mapper包名和位置 -->
        <sqlMapGenerator targetPackage="sqlMap/custom" targetProject="src/main/resources">
        </sqlMapGenerator>

        <!--3、生成DAO的包名和位置 -->
        <javaClientGenerator type="XMLMAPPER"
                             targetPackage="com.sams.custom.mapper" targetProject="src/main/java">
            <property name="enableSubPackages" value="true"/>
        </javaClientGenerator>

        <!-- oracle 使用 schema 对应 用户名称空间 mysql 使用 catalog 对应 数据库 -->
        <!--<table schema="shiro" tableName="%" enableCountByExample="false"-->
        <!--enableDeleteByExample="false" enableUpdateByExample="false"-->
        <!--selectByExampleQueryId="false" enableSelectByExample="false">-->
        <!--<generatedKey column="id" sqlStatement="MySql" identity="true"/>-->
        <!--</table>-->

		<!--4、 配置表映射(更改tableName和domainObjectName即可)-->
        <table tableName="d_fund_quot_tmp" domainObjectName="FundQuotTmp"
		       enableCountByExample="false" enableUpdateByExample="false" 
		       enableDeleteByExample="false" enableSelectByExample="false" 
		       selectByExampleQueryId="false" />
       
    </context>
</generatorConfiguration>

        <!-- 步骤一：maven导入plugin -->
        <!-- <plugin>
            <groupId>org.mybatis.generator</groupId>
            <artifactId>mybatis-generator-maven-plugin</artifactId>
            <version>1.3.1</version>
            <configuration>
                <verbose>true</verbose>
                <overwrite>true</overwrite>
            </configuration>
        </plugin> -->

        <!-- 步骤二：配置generatorConfig.xml -->

        <!-- 步骤三：mvn mybatis-generator:generate 或运行 GenerateUtils.java -->
